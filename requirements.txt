# NOTE: Torch is installed explicitly in the Dockerfile to ensure we get CUDA
# wheels when running on GPU hosts. Keeping it here would allow pip to pull a
# CPU-only wheel from PyPI and override the CUDA build.

gradio
Pillow
lxml

# surya-ocr 0.17.0 requires transformers>=4.56.1
# marker-pdf requires transformers>=4.45.2,<5.0.0
# Pin to the minimum compatible version to avoid unexpected breakage.
transformers==4.56.1

accelerate
hf-transfer
hf-xet
#flash-attn==2.8.3
fastapi
uvicorn
gunicorn
python-multipart
PyMuPDF
apng
img2pdf
# Install marker-pdf with all dependencies
marker-pdf[full]==1.10.1
surya-ocr
sentry-sdk==2.47.0
openai>=1.0.0
httpx>=0.24.0