# Core dependencies
torch>=2.0.0
gradio
Pillow
lxml
transformers
torchvision
accelerate
hf-transfer
hf-xet
fastapi
uvicorn
python-multipart
PyMuPDF
apng
img2pdf
# Install marker-pdf with all dependencies
marker-pdf[full]==1.8.2
surya-ocr
# Dependencies for DotsOCR
qwen-vl-utils
# Flash attention for better performance (requires CUDA dev environment)
# DotsOCR will use SDPA by default, set USE_FLASH_ATTENTION=1 to enable flash-attn
flash-attn