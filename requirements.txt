# Core dependencies
packaging  # Required early for other packages' setup
torch>=2.0.0
gradio
Pillow
lxml
transformers
torchvision
accelerate
hf-transfer
hf-xet
fastapi
uvicorn
python-multipart
PyMuPDF
apng
img2pdf
# Install marker-pdf with all dependencies
marker-pdf[full]==1.8.2
surya-ocr
# Dependencies for DotsOCR
qwen-vl-utils
# Note: flash-attn is NOT installed by default as it requires CUDA build tools
# DotsOCR will use SDPA (built-in PyTorch) instead
# To enable flash attention: set USE_FLASH_ATTENTION=1 and manually install flash-attn